this is combined django project where two projects are combined with different urls --
first for django login system with login middlewares  ** url for login system /api/register.
second for news api ,news comes from apis dynamically  **additionally the url for news api id /api/news.
